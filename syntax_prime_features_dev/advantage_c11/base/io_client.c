#include <stdio.h>
#include <stdlib.h>

int main()
{
    // int ch;
    // while ((ch=getchar()) != EOF)
    // {
    //     putchar(ch);
    // }

    //重定向-同一目录，暂不支持目录
    // FILE *fp;
    // char fname[128];
    // printf("Enter the name of the file:");
    // scanf("%s",fname);
    
    // fp = fopen(fname,"r");
    // if (fp == NULL)
    // {   
    //     printf("Failed to open the file!\n");
    //     exit(1);
    // }

    // while ((ch=getc(fp)) != EOF)
    //     putchar(ch);
    
    // putchar('\n');
    // fclose(fp);

    //处理错误输入:如期望输入long，实际输入字符串
    // long input;
    // char ch;
    // while ((scanf("%ld",&input)) != 1)
    // {
    //     //由于缓冲区保留
    //     while((ch=getchar()) != '\n'){
    //         putchar(ch);
    //     }
    //     printf(" not the target integer,enter again:");
    // }
    // printf("target : %ld\n",input);

    /**
     * GUI模拟
    */

   
    return 0;
}